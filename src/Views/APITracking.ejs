<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>API Usage Tracking</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #121212;
            color: #f4f4f4;
            margin: 0;
            padding: 20px;
            text-align: center;
        }

        .container {
            max-width: 800px;
            margin: auto;
            background: #1e1e1e;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(255, 255, 255, 0.1);
        }

        h1 {
            margin-bottom: 15px;
        }

        .info {
            background-color: #252525;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 12px;
            text-align: left;
            border-left: 4px solid #007bff;
        }

        .log-list {
            max-height: 300px;
            overflow-y: auto;
            background: #2c2c2c;
            padding: 10px;
            border-radius: 5px;
        }

        .log-entry {
            padding: 8px;
            border-bottom: 1px solid #444;
        }

        .log-entry:last-child {
            border-bottom: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>API Usage Tracking</h1>

        <div class="info">
            <p><strong>API Key:</strong> <%= user.apiToken %></p>
        </div>
        
        <div class="info">
            <p><strong>Total Requests:</strong> <%= user.requestCount %></p>
        </div>

        <div class="info">
            <p><strong>Last API Call:</strong> <%= user.lastApiCall ? user.lastApiCall.toLocaleString() : 'Never' %></p>
        </div>

        <div class="info">
            <p><strong>Account Created at:</strong> <%= user.createdAt %></p>
        </div>
    </div>
</body>
</html>
